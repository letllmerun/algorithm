def count_reachable(graph, start):
    visited = set()

    def dfs(node):
        if node not in visited:
            visited.add(node)
            for neighbor in graph[node]:
                dfs(neighbor)

    dfs(start)
    return len(visited)

# пример
print(count_reachable(graph, 'A'))
