def bfs(graph, start):
    visited = set()
    queue = [start]

    while queue:
        node = queue.pop(0)

        if node not in visited:
            print(node, end=' ')
            visited.add(node)
            queue.extend(graph[node])

start = input("Введите стартовую вершину: ")
bfs(graph, start)
