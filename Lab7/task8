def max_element(arr, index=0):
    if index == len(arr) - 1:
        return arr[index]

    max_rest = max_element(arr, index + 1)
    return arr[index] if arr[index] > max_rest else max_rest


print(max_element([3, 7, 2, 9, 5]))  # 9
