class LinkedList:
    def __init__(self):
        self.head = None

    def add_beginning(self, data):
        new_node = Node(data)
        new_node.next = self.head
        self.head = new_node


print("Task 3 result:")
ll = LinkedList()
ll.add_beginning(10)
ll.add_beginning(20)
ll.add_beginning(30)

current = ll.head
while current:
    print(current.data, end=" -> ")
    current = current.next
print("None\n")
